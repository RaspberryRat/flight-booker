<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
      * {
        font: "Helvetica", sans-serif;
        font-size: 20px;

        --dark: #333745;
        --red: #E63462;
        --orange: #FE5F55;
        --green: #C7EFCF;
        --biege: #EEF5DB;
      }

      .container {
        display: flex;
        flex-direction: column;
        align-items: center;
      }

      h2 {
        font-size: 1.5rem;
        padding: 10px;
        font-weight: bold;
        padding-left: 30px;
        }

      .booking-flight-details {
        background-color: var(--red);
        padding: 20px;
        padding-bottom: 30px;
        border-radius: 30px;
        display: flex;
        flex-direction: column;
        color: var(--biege);
        }

      .booking-departure-details {
        margin-bottom: 20px;
      }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
